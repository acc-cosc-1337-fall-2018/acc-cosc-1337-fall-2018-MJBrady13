#include<iostream>
#include "basics.h"

using std::cout;

int main()
{
	const int read_only = 10;
	//read_only = 15;
	//cout << sum_numbers(5, 5);
	//initialize_vector_of_ints();

	initialize_vector_of_strings();

	return 0;
}
